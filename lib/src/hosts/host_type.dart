/// Host type
enum HostType {
  /// API host.
  /// 
  /// API host is used to request JSON data objects.
  api,
  /// Image host.
  /// 
  /// Image host serves full size page images.
  image,
  /// Thumbnail host.
  /// 
  /// Thumbnail host serves page thumbnails and covers.
  thumbnail;
}
