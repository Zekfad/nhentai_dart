/// Model mapper library.
/// It contains model mappers for API objects, such as [BookMapper]s, [TagMapper]s, [ImageMapper]s,
/// and so on.
library nhentai_data_model_mapper;

export 'src/model_mappers.dart';
